name: ai_youtube_auto_editor
description: "AI 스마트폰 영상 자동 편집, 자막 생성, 해시태그 추가 및 유튜브 업로드 크로스플랫폼 앱"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.2.0
  file_picker: ^8.0.0
  url_launcher: ^6.2.5
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
